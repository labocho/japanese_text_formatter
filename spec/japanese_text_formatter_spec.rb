require "spec_helper"

describe JapaneseTextFormatter do
  it "has a version number" do
    expect(JapaneseTextFormatter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
